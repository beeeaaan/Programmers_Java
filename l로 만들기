class Solution {
    public String solution(String myString) {
        
        // replaceAll(정규식, 바꿀 문자) : 문자열 전부 치환
        String answer = myString.replaceAll("[a-k]", "l");
        return answer;
    }
}
