class Solution {
    public String solution(String myString) {
        String answer = "";
        
        // myString.toUpperCase : 대문자 변환
        answer = myString.toUpperCase();
        return answer;
    }
}
