class Solution {
    public int[] solution(String[] strlist) {
        int[] answer = new int[strlist.length];
        
        for(int i = 0; i < strlist.length; i++) {  // 배열 번호
            answer[i] = strlist[i].length();  // 각 인덱스별 문자열 길이
        }
        return answer;
    }
}
