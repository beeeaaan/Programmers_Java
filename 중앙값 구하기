import java.util.*;
class Solution {
    public int solution(int[] array) {
        int answer = 0;
        
        Arrays.sort(array); // 정렬
        answer = array[array.length / 2]; // 중앙값
        return answer;
    }
}
