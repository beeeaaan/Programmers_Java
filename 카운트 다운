class Solution {
    public int[] solution(int start, int end) {
        int[] answer = new int[start - end + 1];
        
        for(int i = 0; i <= start - end; i++) {  // 인덱스 0부터 시작
            answer[i] = start - i;  // start부터 시작하기 위해
        }
        return answer;
    }
}
