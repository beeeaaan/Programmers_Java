class Solution {
    public int solution(int[] array, int n) {
        int answer = 0;
        
        for(int i = 0; i < array.length; i++) {
            if(array[i] == n) {  // n과 일치하는 경우
                answer++;  // 일치하는 n의 갯수
            }
        }
        return answer;
    }
}
