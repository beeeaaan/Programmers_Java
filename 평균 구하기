class Solution {
    public double solution(int[] arr) {
        double answer = 0;
        int sum = 0;
        
        for(int i = 0; i < arr.length; i++) {
            sum += arr[i]; // 배열의 합
        }
        answer = (double)sum / arr.length;  // 평균값
        return answer;
    }
}
