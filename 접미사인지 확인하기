class Solution {
    public int solution(String my_string, String is_suffix) {
        int answer = 0;
        
        // endsWith : 대상 문자열이 특정 문자 또는 문자열로 끝나는지 체크(true/false)
        if(my_string.endsWith(is_suffix)) {
            answer = 1;
        }
        return answer;
    }
}
