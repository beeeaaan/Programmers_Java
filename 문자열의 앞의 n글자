class Solution {
    public String solution(String my_string, int n) {
        String answer = "";
        
        // substring : 문자열 자르기(시작, 끝)
        answer = my_string.substring(0, n);
        return answer;
    }
}
