class Solution {
    public int solution(int[] array, int height) {
        int answer = 0;
        
        for(int i = 0; i < array.length; i++)  // 키순으로 줄서기
            if(array[i] > height) {  // 머쓱이보다 큰 사람
                answer++;
        }
        return answer;
    }
}
