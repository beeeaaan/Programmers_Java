class Solution {
    public int solution(int number, int n, int m) {
        int answer = 0;
        
        if(number % n == 0 && number % m == 0) {
            answer = 1; // n, m의 배수면
        } else {
            answer = 0; // n, m의 배수가 아니면
        }
        return answer;
    }
}
