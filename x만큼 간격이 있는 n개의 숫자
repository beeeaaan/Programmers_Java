class Solution {
    public long[] solution(int x, int n) {
        long[] answer = new long[n];
        
        for(int i = 0; i < n; i++) { // x부터 시작
            answer [i] = (long)x * (i+1); // x씩 증가
        }
        return answer;
    }
}
