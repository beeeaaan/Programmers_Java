class Solution {
    public int[] solution(int start, int end) {
        int[] answer = new int[end - start + 1];
        
        for(int i = 0; i <= end - start; i++) {  // 인덱스 0부터 시작
            answer[i] = i + start;  // start부터 시작하기 위해
        }
        return answer;
    }
}
