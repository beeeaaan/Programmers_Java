class Solution {
    public int solution(int n) {
        int answer = 0;
        
        for(int i = 2; i < n; i++) {  // i = 1 입력시 오류(나머지)
            if(n % i == 1) {  // 나머지 1
                answer = i;
                break;
            }
        }
        return answer;
    }
}
