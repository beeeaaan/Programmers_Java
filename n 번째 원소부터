import java.util.Arrays;
class Solution {
    public int[] solution(int[] num_list, int n) {
        // Arrays.copyOfRange(원본, 복사 시작 위치,  배열의 크기)
        int[] num_arr = Arrays.copyOfRange(num_list, n - 1, num_list.length);
        return num_arr;
    }
}
